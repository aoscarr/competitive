// Dijkstra so resolve problemas com arestas de pesos positivos!
// Cuidado com ciclos negativos (exemplo 1 anotado).

void dijkstra(int v){
   for(i = 0; i < n; i++)
      dist[i] = -1;
      
   set<pii> fila;
   fila.insert({0,v});
   pii aux;
   while(!fila.empty()){
      aux = *fila.begin();
      fila.erase(fila.begin());
      int 
   }
   
}
