//https://codeforces.com/group/QlsCcB22aH/contest/205217/problem/D
#include <bits/stdc++.h>
using namespace std;
#define desync ios_base::syns_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
#define endl '\n'
#define pb push_back
#define ff first
#define ss second
#define pii pair<int,int>
#define vii vector<pii>

int main(){
     int h,m,h_vez,m_vez, cont = 0; cin >> h >> m;
     queue<int> homens, mulheres;
     vector<string>ans;
     for(int i =1 ; i <= h; i++)
          homens.push(i);
     for(int i =1 ; i <= m; i++)
          mulheres.push(i);

     while(!homens.empty() && !mulheres.empty()){

     }
}
