#include <bits/stdc++.h>
using namespace std;
#define desync ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL)
#define endl '\n'
#define pb push_back

int main(){
	desync;
	int n,m, aux;

	cin >> n >> m;

	vector<pair<int,int>> intervalos;
	vector<pair<int,int>>::iterator it;
	vector<int,int> mapa;

	for(int i =0; i < n-1; i++){
		cin >> aux;
		v.pb(make_pair(aux, 0));
	}
	for(int i )
	for(int i =0; i < n; i++){
		cin >> aux;
		v[i].second = aux;
	}
	for(int i =0; i < m; i++){
		cin >> aux;
		int ini = 0, fim = intervalos.size()-1;
		if(intervalos[fim/2].first > aux)
	}
	cout << endl;

}